[project]
name = "resume-screening-v2"
version = "1.0.0"
description = "Resume Screening Application"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy"
]

[tool.vercel]
entrypoint = "backend.app.main:app"